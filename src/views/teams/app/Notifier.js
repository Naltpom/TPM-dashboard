import { Card, CardHeader, CardBody, CardTitle, CardText, CardLink } from 'reactstrap'

const Notifier = () => {
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>An app</CardTitle>
        </CardHeader>
        <CardBody>
          <CardText>See all features of the app</CardText>
        </CardBody>
      </Card>

    </div>
  )
}

export default Notifier
