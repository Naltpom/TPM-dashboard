import { Card, CardHeader, CardBody, CardTitle, CardText, CardLink } from 'reactstrap'

const Preview = () => {
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>Preview</CardTitle>
        </CardHeader>
        <CardBody>
          <CardText>Team dashboard</CardText>
        </CardBody>
      </Card>

    </div>
  )
}

export default Preview
