import { Card, CardHeader, CardBody, CardTitle, CardText, CardLink } from 'reactstrap'

const Create = () => {
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>Create tickets</CardTitle>
        </CardHeader>
        <CardBody>
          <CardText>Here is the place to create a ticket</CardText>
        </CardBody>
      </Card>

    </div>
  )
}

export default Create
