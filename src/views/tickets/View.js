import { Card, CardHeader, CardBody, CardTitle, CardText, CardLink } from 'reactstrap'

const AllTickets = () => {
  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>View ticket</CardTitle>
        </CardHeader>
        <CardBody>
          <CardText>Read a ticket to anwser it</CardText>
        </CardBody>
      </Card>

    </div>
  )
}

export default AllTickets
